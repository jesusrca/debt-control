.PHONY: dev build start test db:init db:seed lint clean

dev:
	npm run dev

build:
	npm run build

start:
	npm run start

test:
	npm run test

lint:
	npm run lint

db:init:
	npm run db:init

db:seed:
	npm run db:seed

clean:
	rm -rf dist/
	rm -f data/*.db